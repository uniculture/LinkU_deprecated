machine:
  pre:
    - cd /opt/circleci/.pyenv; git pull    
  python:
    version: 3.6.0

dependencies:
  pre:
    - pip install -r requirements.txt
  post:
    - ./prepare_functionaltest.sh :
        background: true 
test:
  override:
    - pytest --cov=./foodgroup/ foodgroup/functional_tests/selenium-browser.py
  post:
    - codecov

